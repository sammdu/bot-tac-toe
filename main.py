#!/usr/bin/env python3
"""
Imports the `webbrowser` module and opens the `index.html` file of the current directory
with the default web browser, in a new tab.
"""
import webbrowser
webbrowser.open_new_tab("./index.html")
